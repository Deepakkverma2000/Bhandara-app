import 'package:flutter/material.dart';

/// Religious / temple-inspired palette for Bhandara Live
class AppColors {
  // Bhagwa (saffron)
  static const saffron = Color(0xFFFF9933);
  static const deepSaffron = Color(0xFFE65100);
  static const lightSaffron = Color(0xFFFFCC80);

  // Maroon & temple red
  static const maroon = Color(0xFF7B1A1A);
  static const deepMaroon = Color(0xFF4A0E0E);
  static const templeRed = Color(0xFFC62828);

  // Gold accents
  static const gold = Color(0xFFD4AF37);
  static const lightGold = Color(0xFFFFE082);

  // Backgrounds
  static const cream = Color(0xFFFFF8E7);
  static const lightPeach = Color(0xFFFFF3E0);
  static const cardPeach = Color(0xFFFFECB3);

  // Legacy aliases (used across app)
  static const primaryOrange = saffron;
  static const deepOrange = deepSaffron;
  static const darkGreen = Color(0xFF1B5E20);
  static const bannerGreen = maroon;
  static const liveRed = templeRed;
  static const textDark = Color(0xFF3E2723);
  static const textMuted = Color(0xFF6D4C41);
  static const iconBg = Color(0xFFFFE0B2);

  static const heroGradient = [
    Color(0xFFFF9933),
    Color(0xFFE65100),
    Color(0xFF7B1A1A),
  ];
}
